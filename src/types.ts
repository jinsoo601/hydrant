// file that exports commonly used types in the project
export type Post = {
  content: string,
  timestamp: number
};

export type Theme = {
  colorPrimary: string,
  colorSecondary: string,
  hoverPrimary: string,
  textLight: string,
  textDark: string,
  topBarHeight: number
};